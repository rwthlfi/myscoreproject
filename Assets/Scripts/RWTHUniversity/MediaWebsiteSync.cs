using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Do we need this. ?
//the Website is an Local function anyway.

//to be decided....
public class MediaWebsiteSync : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
