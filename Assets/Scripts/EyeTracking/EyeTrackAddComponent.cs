using UnityEngine;
//using Tobii.XR;

public class EyeTrackAddComponent : MonoBehaviour
{
    /*
    public bool addBoxCollider;

    void Start()
    {
        // Attach this script to any empty object that needs to be used for eye tracking, if the object doesn't have a collider turn on the box collider
        // For it to work better, add a box collider to an empty object, change the scaling to be perfect for your object and remove the box collider, therefore the created collider will have the right scale

        if (TobiiXR.Internal.Provider != null && TobiiXR.Internal != null)
        {
            if (TobiiXR.Internal.Provider.GetType().Name == "HTCProvider")
            {
                gameObject.AddComponent<EyeTrackFocusTrigger>();

                if (addBoxCollider) // add if trackable object does not have a collider on its own
                {
                    BoxCollider bc = gameObject.AddComponent<BoxCollider>() as BoxCollider;
                    bc.isTrigger = true;
                }
            }
        }
    }
    */
}